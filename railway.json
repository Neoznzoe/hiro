{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma db push --accept-data-loss && npm start -- -p ${PORT:-3000}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
